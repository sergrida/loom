#@es Qué es OpenSSH.
#@en What is OpenSSH.
echo -e "openssh (es)         - OpenSSH es un conjunto de aplicaciones que permiten realizar comunicaciones cifradas a través de una red, usando el protocolo SSH."
echo -e "openssh (en)         - OpenSSH is a set of applications that allow encrypted communications over a network, using the SSH protocol."
whatis ssh
